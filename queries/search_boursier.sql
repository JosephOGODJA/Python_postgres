-- This query is intended for making a research

-- The research by specifying the full boursier's name has been implemented through the show_boursier.sql script. We're just implemeting the ordering script in case the user wanted to sort the list by specific field.

SELECT * FROM boursier ORDER BY ;-- The column will be provided by the user
